# Serving only — no training dependencies
# Use this on Render/Railway free tier to avoid build timeouts
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.0.0
pydantic>=2.4.0
pandas>=2.0.0
numpy>=1.24.0
python-dateutil>=2.8.2
